
spring.profiles.active=dataSource

server.port=8060
spring.application.name=security-server
#把注册中心的地址换成linux上docker中的注册中心
eureka.client.service-url.defaultZone=http://10.0.0.61:8761/eureka/
#eureka.client.service-url.defaultZone=http://peel1:8761/eureka/

#spring.devtools.restart.enabled=true
#spring.devtools.restart.additional-paths=src/main/java,src/main/resources/static,src/main/resources/templates
#spring.devtools.restart.poll-interval=3s
#spring.devtools.restart.quiet-period=480ms

#此处locations后面少了s导致一直扫不到mapper
mybatis.mapper-locations=classpath*:mapper/*.xml
mybatis.type-aliases-package=com.zc.loginserver.domain.entity